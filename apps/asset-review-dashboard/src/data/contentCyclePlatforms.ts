import type { CatalogSelectOption } from "../types/catalogs";

/** Publication platforms available for the content cycle (not in admin catalogs yet). */
export const contentCyclePlatforms: CatalogSelectOption[] = [
  { value: "instagram", label: "Instagram" },
];
